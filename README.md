# tonhowtf/homebrew-tap

```bash
brew install --cask tonhowtf/tap/omniget
```

The `omniget` cask is regenerated automatically by the [OmniGet release workflow](https://github.com/tonhowtf/omniget/blob/main/.github/workflows/homebrew.yml) whenever a release is published.
